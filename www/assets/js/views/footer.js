define(['backbone'],function (Backbone) {    

    var Footer = Backbone.View.extend({
        el : $('footer'),
        events : {
            
        }
    });

    return Footer;
    
});